==TODO==


==GENERAL PIRE ===

* Output array is now expected to be pointer to <whatever we want to have as result>, for instance an array.
  This means that EnqeueReaderBuffer expects an address, so we need to dereference it ==> *out instead of passing out.
===>  When we use EnqueWriteBuffer, we should do & (NYI).

* Use local memory in reduce kernels instead of global

* Control group/workitem size (Discuss w/ Joel)






